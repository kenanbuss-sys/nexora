# Sprint 002 — Configuration, Workflow, Rules & Documents

Implement versioned tenant config, terminology, feature flags, custom-field foundation, tasks/inbox, workflow state engine, approval primitive and document-template foundation. Stable domain/API primitives before a full visual workflow designer.
