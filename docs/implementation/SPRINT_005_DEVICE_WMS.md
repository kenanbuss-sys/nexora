# Sprint 005 — Device/Verification MVP + WMS Execution

Device registry, generic scanner/print boundary, Android reference adapter boundary, offline envelope, receiving, transfers, counts and basic pick/pack. Never bind business logic to a hardware model.
