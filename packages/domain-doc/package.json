{
  "name": "@nexora/domain-doc",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "main": "./dist/public.js",
  "types": "./dist/public.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@nexora/audit": "workspace:*",
    "@nexora/db": "workspace:*",
    "@nexora/kernel": "workspace:*",
    "@nexora/tenancy": "workspace:*",
    "pdfkit": "^0.20.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pdfkit": "^0.17.6",
    "typescript": "^5.9.0",
    "vitest": "^4.1.0"
  }
}
