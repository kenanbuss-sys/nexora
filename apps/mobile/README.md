# @nexora/mobile — placeholder

React Native app for mobile operations (receive, putaway, count, pick, pack,
work-order operations, verification) with native-module support, Android
operational devices first — per `docs/architecture/00_DECISIONS_LOCKED.md`.

The React Native project is initialized in **Sprint 005** (device + verification
MVP + warehouse execution). Keeping the placeholder package here reserves the
workspace location without adding heavy native toolchain requirements to the
foundation CI.
